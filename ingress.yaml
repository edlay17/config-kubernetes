apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: php-web-app-ingress
spec:
  rules:
  - host: your-app-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-app-service
            port:
              number: 80
